hyperfine 'openssl speed rsa'
# このコマンドは、RSA暗号化処理の速度を測定し、平均実行時間、標準偏差、最小/最大実行時間などの統計情報を表示します。

# 他のCPU負荷の高いコマンド（例: ffmpeg、imagemagick）でも同様にテストできます。

# 3. ディスクI/O性能のテスト

# ディスクI/O性能をテストするには、ディスクへの読み書きを行うコマンドを実行し、その実行時間を測定します。例えば、ddコマンドを使用して、ディスクへの書き込み速度を測定できます。

hyperfine 'dd if=/dev/zero of=test.img bs=1M count=1024'
# このコマンドは、1GBのデータをディスクに書き込み、その実行時間を測定します。

# ディスクからの読み込み速度を測定するには、ifオプションにディスク上のファイルを指定します。

# 4. メモリ性能のテスト

# メモリ性能をテストするには、メモリ負荷の高いコマンドを実行し、その実行時間を測定します。例えば、shredコマンドを使用して、ファイルをメモリで上書きする速度を測定できます。


hyperfine 'shred -n 1 -v test.img'
# このコマンドは、test.imgファイルをメモリで1回上書きし、その実行時間を測定します。

# 5. 複数のコマンドを比較する

# hyperfineコマンドは、複数のコマンドの実行時間を比較することもできます。例えば、異なる圧縮アルゴリズムの性能を比較するには、以下のコマンドを実行します。


hyperfine 'gzip -c test.txt > /dev/null' 'bzip2 -c test.txt > /dev/null' 'xz -c test.txt > /dev/null'

# このコマンドは、gzip、bzip2、xzの各コマンドでtest.txtファイルを圧縮し、その実行時間を比較します。

# 6. 結果の解釈

# hyperfineコマンドの出力結果には、平均実行時間、標準偏差、最小/最大実行時間などの統計情報が表示されます。これらの情報を参考に、PCの性能を評価してください。

# 平均実行時間: コマンドの平均的な実行時間
# 標準偏差: 実行時間のばらつき
# 最小/最大実行時間: 実行時間の最小値と最大値
# これらの情報を総合的に判断して、PCの性能を評価します。

# 注意点:

# ベンチマーク結果は、PCの構成や実行環境によって異なります。
# 複数のベンチマークツールを使用して、結果を比較することを推奨します。
# ベンチマーク結果は、あくまで目安として捉え、実際の使用状況に合わせて評価してください。
# これらの情報を参考に、hyperfineコマンドを使用してPCの性能をテストしてみてください。